"""소스 기반 route 추출 → vc_map_routes (7.1절). Day1.

7.1절 소스 기반 신호: Controller/route decorator, security config, ORM query, template
output, upload handler, CORS, dependency manifest.

**P2의 target이 뜨지 않아도 소스만 있으면 동작한다** — Day1에 아무에게도 안 막히는 작업.
동적 크롤(scanners/playwright.py)과 합쳐 graph.py가 최종 그래프를 만든다.
"""
