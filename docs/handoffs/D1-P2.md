# D1 / P2 Handoff

## 상태
진행 중 — P2 foundation 및 P1 공통 계약 호환(target registration bridge/catalog)을 구현했고, 실제 target 48개 intake를 마쳤다. Docker Desktop을 D: 데이터 디스크로 이전했고 8개 target의 build/start/health/regression/reset을 실제 통과시켰다. `26s-w1-c1-01`은 health-level 실행까지 통과했지만 source DB schema 부재로 기능 회귀를 완료하지 못했고, 사용자 지시에 따라 가능한 target 우선 진행 대상으로 보류했다.

## 변경 파일
- `adapters/`: Spring Boot/FastAPI/Node/generic Docker가 공유하는 manifest-driven adapter interface와 registry.
- `runtime/manifest.py`: loopback-only target manifest schema와 YAML loader.
- `runtime/lifecycle.py`: fixed argv command runner, approved reset, health check, test suite result.
- `runtime/worktree.py`: run-scoped detached Git worktree 생성/승인 삭제.
- `runtime/registration.py`, `runtime/catalog.py`, `runtime/readiness.py`: manifest → P1 `Target` 변환, `Target.id` 기반 registry, non-mutating readiness/log location inspection.
- `runtime/test_runner.py`: run ID로만 조회하는 Git worktree regression suite runner.
- `runtime/target_service.py`: P1 policy/evidence contract를 사용하는 target registration/build/start/reset service.
- `runtime/compose_isolation.py`: Docker Compose loopback port/internal network/non-privileged static isolation verifier.
- `runtime/manifest.py`, `runtime/lifecycle.py`: trusted command별 optional `working_dir`를 추가해 source clone 밖의 추적 P2 Compose overlay를 안전하게 실행. healthcheck의 명시적 expected HTTP status(예: 인증 보호 endpoint의 401)도 준비 상태로 판정.
- `mcp_server/tools_inventory.py`: P1이 제공한 schema shell에 P2 inventory/lifecycle 구현을 연결.
- `targets/example-fastapi.yaml`, `targets/README.md`: manifest example과 운영 규칙.
- `targets/compose/`, `targets/dockerfiles/`, `targets/manifests/`, `targets/nginx/`, `targets/scripts/`: 실제 local target Compose/Dockerfile/manifest/nginx proxy/smoke artifacts. 현재 `26s-w1-c1-01`~`07`, `26s-w1-c2-01`~`02`를 추가.
- `tests/`: manifest/lifecycle/worktree security and behavior tests.
- `requirements.txt`, `.env.example`, `.gitignore`: YAML parser와 tracked example env configuration.
- `.vibecutter/targets/sources/` (gitignore된 로컬 작업 경로): `madcamp-official`의 이름에 `w1` 또는 `w2`가 포함된 48개 repository shallow clone. 대상 원본에는 아직 변경하지 않음.
- `docs/handoffs/D1-P2.md`: 실제 target intake, 실행 검증 및 계약 결정 기록.

## 제공 인터페이스
- 입력: validated `TargetManifest` (`id`는 P1 `Target.id`와 동일), repository root, fixed `command_id`; reset/remove에는 `approved=True`.
- 출력: `CommandResult(status, exit_code, stdout, stderr, duration_ms)`, `HealthResult(status, attempts, observed_status, reason)`.
- regression 입력/출력: `RunScopedTestRunner.run(run_id)` → `TestRunSummary(status, results, worktree_path)`; test suite가 없으면 `not_configured`이며 통과로 처리하지 않음.
- lifecycle MCP: `vc_register_target(manifest)`, `vc_inspect_stack(target_id)`, `vc_check_readiness(target_id)`, `vc_build_target(target_id)`, `vc_start_target(target_id)`, `vc_reset_target(target_id, approved)`.
- Docker readiness: `docker_isolation` manifest block을 선언한 target은 egress-blocked network(`internal: true` 또는 bridge NAT masquerade 비활성화), service network membership, loopback port binding, `privileged`/`network_mode` 금지를 실행 전에 검사.
- 실패/예외: 외부 base URL, repository escape path, shell syntax, 미등록 command ID, 승인 없는 reset/delete는 거부.

## 검증
- `py -3.13 -m unittest discover -s tests -v`: 31 passed.
- `targets/example-fastapi.yaml` YAML parse: passed.
- 실제 repository에서 `.vibecutter/worktrees/p2-smoke` create → approved remove: passed.
- manifest hash와 `allowed_hosts`, adapter/tool version을 P1 `Target`/`Run` metadata로 변환: passed.
- source/executable/role fixture/log metadata readiness inspection: passed (secret/log content 미노출 확인).
- 임시 Git repository의 P2-managed worktree에서 regression test 실행, 없는 run worktree 거부, empty suite `not_configured`: passed.
- checked-in manifest equality, scope host/port, typed command policy, build→READY Run 저장, reset approval gate: passed.
- `from mcp_server.server import mcp`: passed (stdio server import smoke test).
- compliant Compose 통과, non-loopback/privileged/non-internal Compose 거부, compose file missing 거부: passed.
- w1/w2 전체 대상 intake: 48개 shallow clone 완료, `git rev-parse --is-inside-work-tree` 48/48 passed.
- 구조 점검: 48개 중 Compose 사용 repository 20개(Compose 파일 23개), Dockerfile 보유 repository 13개, README health 단서 보유 repository 19개.
- Compose 사전점검: 23개 모두 현재 `internal: true` network와 loopback-only published port 요건을 만족하지 않아, 원본 Compose를 그대로 시작하지 않고 target별 격리 overlay/Compose를 먼저 작성해야 함. `privileged: true` 및 `network_mode`는 발견하지 못함.
- Docker Desktop 4.82.0 / Engine 29.6.1 / Compose v5.3.0을 설치·기동했고, Python 3.13에 `requirements.txt`를 설치. `py -3.13 -m unittest discover -s tests -v`: 31 passed.
- `26s-w1-c1-02`: local Postgres + source migration/seed + Node backend Compose build passed; `http://127.0.0.1:14003/health` 200; 내장 Socket.IO multiplayer regression 전 항목 passed; approved `down --volumes`로 containers/network/volume 제거 확인. Notion 완료 체크 반영.
- `26s-w1-c1-04`: 추적 Dockerfile + Node Compose build passed; `http://127.0.0.1:14004/api/leaderboard` 200; leaderboard create/read smoke passed; approved reset으로 container/network 제거 확인. Notion 완료 체크 반영.
- `26s-w1-c1-01`: local Postgres + backend/frontend Compose build passed; `http://127.0.0.1:14001/health` 200 및 `http://127.0.0.1:14002/` 200; approved reset passed. 그러나 `GET /countries`는 원본이 요구하는 외부 Supabase schema/migration artifact 부재로 500을 반환했다. 기능 regression/Notion 완료 체크는 보류.
- `26s-w1-c1-03`: local Postgres + Spring Boot Compose build passed (Spring Boot 4.1 요구에 맞춰 Gradle 8.14); `http://127.0.0.1:14005/api/teams` 401 loopback health; invalid login 400; isolated source Gradle regression `test` exit 0 (1m 39s); approved reset으로 app/database/test container, network, volume 제거 확인. Notion 완료 체크 반영.
- `26s-w1-c1-05`: local MySQL + Spring Boot + React/Nginx Compose build passed; `http://127.0.0.1:14006/api/health` 200, `http://127.0.0.1:14007/` 200; local ephemeral user signup 201 → login 200 → authenticated `/api/me` 200; isolated Spring/H2 regression `test` exit 0 (57s); approved reset으로 app/frontend/database/test container, network, volume 제거 확인. Gemini key 없이 source fallback mode로 실행. Notion 완료 체크 반영.
- `26s-w1-c1-06`: local PostgreSQL 16 + source `schema.sql` one-shot apply (public tables 12개) + Node API + React/Nginx same-origin proxy Compose build passed; `http://127.0.0.1:14008/api/health` 200, `/api/db/health` 200, `http://127.0.0.1:14009/` 200; Nginx proxy 경유 ephemeral user signup 201 → login 200 → authenticated `/api/auth/me` 200; Node workspace regression 45 tests passed (shared 27 + API 18); approved reset으로 app/frontend/schema/database/test container, network, volume 제거 확인. Notion 완료 체크 반영.
- `26s-w1-c1-07`: local MySQL 8 + source Prisma migration 4개 + idempotent game/score seed + single Fastify/Socket.IO server와 production React 정적 파일 Compose build passed; `http://127.0.0.1:14010/api/health` 200, anonymous normal path `/api/me` 200; shared/server/client TypeScript typecheck 및 standalone guard passed. 최초 run의 local volume을 approved reset으로 제거하고 새 volume에서 migration/seed/health를 재현했다. Google OAuth는 실제 계정·token 없이 실행하지 않았다.
- `26s-w1-c2-01`: local pgvector/PostgreSQL 16 + source extension init SQL + Alembic migration + Redis + FastAPI/React-Nginx Compose build passed; `http://127.0.0.1:14011/api/v1/health/ready` 200, signup 201 → login 200, same-origin frontend proxy ready 200, UI 200; Python compile regression passed. failed initial volume을 approved reset으로 제거하고 fresh volume에서 extension/migration/health를 재현했다. Gemini worker/recommendation은 egress 제한 때문에 실행하지 않았다.
- `26s-w1-c2-02`: local PostgreSQL 16 + source Prisma migration 6개 + Node/Express + React/Nginx Compose build passed; `http://127.0.0.1:14013/api/health` 200, frontend 및 same-origin `/api` proxy 200, local ephemeral signup 201; containerized backend TypeScript build passed. approved `down --volumes` reset 후 fresh volume에서 migration 및 API/UI health 200을 재현했다. DB password와 access token은 저장하지 않았다.
- `26s-w1-c2-03`: FastAPI + React/Nginx 격리 runtime build passed; `http://127.0.0.1:14015/health`, UI, same-origin proxy 모두 200이고 backend pytest 28 passed, reset 후 health 재현. 다만 원본에 local Supabase schema/service가 없고 외부 OAuth·Kakao/NTS API가 필요한 관계로 `supabase_connected: false` 상태의 health/test runtime까지만 검증했으며, 정상 DB 기능·Notion 완료 체크는 보류했다.
- 아직 실제 환경에서 검증하지 못한 항목: 사용자 지시에 따라 보류한 `26s-w1-c1-01`의 source-equivalent local schema/seed와 정상 기능 regression, 나머지 40개 target의 Docker build/start/health/reset, target별 P1 scope policy 등록 및 P1 MCP tool 실연동.

## 다른 역할에 필요한 사항
- P1: target allowlist/registration tool에서 `TargetCatalog(manifest_root=targets/manifests, repository_root=...)`를 초기화한 뒤 `catalog.get(target_id).contract_target`, `catalog.readiness_for(target_id)`, `catalog.lifecycle_for(target_id)`, `catalog.adapter_for(target_id)`, `catalog.test_runner_for(target_id)`를 호출할 연결 지점 필요. MCP caller에는 raw URL/command 대신 target_id와 lifecycle operation만 노출 필요.
- P1: `mcp_server/tools_inventory.py`의 P2 lifecycle body를 연결했다. 실제 target을 추가할 때는 target manifest와 함께 `scope.yaml`의 host/port, `commands.yaml`의 build/start/reset typed command policy를 추가해야 한다.
- P1: 실제 통과 target의 allowlist 후보는 `26s-w1-c1-02: 127.0.0.1:14003`, `26s-w1-c1-03: 127.0.0.1:14005`, `26s-w1-c1-04: 127.0.0.1:14004`, `26s-w1-c1-05 API: 127.0.0.1:14006` (UI: `127.0.0.1:14007`), `26s-w1-c1-06 API: 127.0.0.1:14008` (UI: `127.0.0.1:14009`), `26s-w1-c1-07: 127.0.0.1:14010`, `26s-w1-c2-01 API: 127.0.0.1:14011` (UI: `127.0.0.1:14012`), `26s-w1-c2-02 API: 127.0.0.1:14013` (UI: `127.0.0.1:14014`)이다. manifest는 추가됐지만 P2는 P1 소유 `policies/`를 수정하지 않았다.
- P4/target inventory: 실제 Docker Compose target은 manifest `docker_isolation` block과 compose file을 함께 제공해야 readiness가 통과한다.
- P3: candidate replay/regression 시 P2 `test_suites` 결과와 run worktree path를 소비. 패치 apply 전에는 P1 approval gate가 `approved=True`을 전달해야 함.
- P4: 실제 실행 가능한 target repository와 stack·seed·role fixture·health endpoint 정보를 manifest 형태로 제공 필요.
- 사용자/환경: Docker Desktop(WSL 2 backend 포함)을 설치·기동해야 target별 Docker build → loopback health → approved reset을 실제로 진행할 수 있음.

## 결정·가정·리스크
- manifest의 `id`는 공통 `Target.id`와 정확히 일치하며, manifest hash·loopback host:port·adapter는 P1 `Target`으로 변환된다. adapter tool version은 P1 `Run.tool_versions`에 복사한다.
- 공통 MCP schema 변경: `vc_reset_target`에 required `approved: bool`을 추가했다. 이전 `target_id` 단독 호출은 거부되며, destructive reset의 explicit approval 원칙을 구현하기 위한 비호환 변경이다.
- Compose static verifier는 Docker daemon을 실행하지 않는다. Docker Desktop에서 `internal: true` bridge가 published loopback ingress도 차단함을 실제 재현했다. 따라서 host loopback base URL을 유지해야 하는 target에는 Docker bridge NAT masquerade를 `false`로 설정하는 동등 egress-blocked 구성을 허용한다. 이 변경은 P2 manifest/README/runtime contract의 호환성 결정이며, P1에는 위 allowlist 등록 시 이 의미를 인지할 필요가 있다.
- manifest만으로 target이 승인되지는 않으며 P1 scope allowlist와 결합되어야 한다.
- health check는 loopback HTTP만 사용하고 target-controlled redirect도 추적하지 않는다.
- source가 valid readiness endpoint를 제공하지 않을 경우 manifest는 해당 endpoint의 명시적 안정 HTTP response를 health verdict로 기록할 수 있다. 이때 2xx 이외 response도 `expected_status`와 정확히 일치할 때만 ready이며 redirect는 계속 거부한다.
- `26s-w1-c1-05`는 external Gemini 호출을 원래 source fallback configuration으로 비활성화하고, runtime network 자체도 NAT masquerade를 끈 bridge로 제한했다. source DB/data는 reset volume 안에만 생성한다.
- `26s-w1-c1-06` frontend는 source의 `/api` same-origin contract를 유지하는 tracked Nginx proxy를 통해 local Node API로만 전달한다. source `schema.sql`은 fresh reset volume에서만 one-shot 적용하며, 외부 Neon/VM/Cloudflare endpoint를 사용하지 않는다.
- `26s-w1-c1-07`은 source의 deployment shape(단일 Fastify server가 client `dist`도 서빙)를 유지한다. Google OAuth에는 local-only public placeholder를 사용하고 실제 OAuth credential/계정은 fixture나 evidence에 넣지 않는다. MySQL healthcheck는 socket ping이 아니라 TCP `127.0.0.1:3306`을 검사해 image entrypoint의 임시 DB 서버를 ready로 오인하지 않는다.
- `26s-w1-c2-01`은 source가 제공한 pgvector extension init SQL을 fresh local DB에만 mount한다. Gemini worker는 local egress 차단 규칙과 충돌하므로 서비스 구성에서 제외하고, API의 Gemini key/model은 빈 값으로 고정한다.
- example manifest는 문서용이며 실제 compose file·credential을 포함하지 않는다.
- P2는 `contracts/`, `core/`, `policies/`를 변경하지 않았다. `PyYAML` 추가는 manifest YAML parsing을 위한 runtime dependency다.
- Notion의 repository-level 체크는 clone 또는 README 확인만으로 완료하지 않는다. target별 격리 Compose/manifest, build, loopback health, approved reset이 모두 확인된 뒤에만 체크한다.
- `working_dir`는 target source가 아닌 repository 내 추적 orchestration 파일을 고정 argv로 실행할 때만 쓰며, 절대 경로·상위 경로 탈출을 거부한다.
