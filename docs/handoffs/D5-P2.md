# D5 / P2 Handoff

## 상태

P2 독립 구현 완료 / P1 배선·P3 실앱 run 대기 — main `63e5ffc`에서 manifest/worktree/
overlay/reset/sweep/test-runner에 source lock/bootstrap, GPU worker preflight, write verifier
baseline restore를 추가했다. 세 GPU의 20개 runtime도 같은 main으로 갱신해 source revision,
Docker, static readiness, listening port를 다시 확인했다. 이후 P2는 독립 audit operator가 아니라,
P3가 허가된 20개 target을 검증할 때 provisioning/fixture/runtime 문제를 푸는 지원 역할을 맡는다.

## 2026-07-20 종합 재점검

- **P1 배선 완료:** `mcp_server/driver.py`가 batch 시작 전 stale overlay sweep, target build/start,
  candidate별 worker Run, target별 순차 실행, patch overlay 생성 worker의 `reset_run`을 호출한다.
  `Candidate.origin_candidate_id`와 기존 SQLite DB additive migration도 main에 반영됐다.
- **P3 접점 완료:** worker Run으로 복제된 Candidate를 verifier가 처리하며, `c1-05`는 실제
  `verified → fixed` closed-loop를 완주했다. `c2-04` read/write 후보 3개는 실제 음성으로
  `rejected`가 기록됐다. `c2-01`, `c2-02`, `c1-06` self-signup hint도
  `surface/candidates.py`에 연결돼 있다.
- **허가 범위:** GPU queue의 20개 코딩캠프 서비스는 모두 승인된 scan/verify 대상이다.
  일부 앱에서 Injection/XSS 후보가 0건이었던 것은 권한 문제가 아니라 현재 prefilter가 찾은
  실제 패턴이 없었다는 결과다. P3는 20개 전체를 계속 검사할 수 있다.
- **source 상태:** `targets/source-lock.yaml`이 manifest 22개를 canonical GitHub repository와
  exact 40자 commit에 1:1로 고정한다. 로컬 22개 clone은 모두 `ready`이며, contract `Target`의
  `source_commit`과 run worktree도 이 revision을 사용한다. `runtime/source_bootstrap.py`는
  승인된 `target_id`로 누락 clone만 생성하고, dirty/origin/revision mismatch 기존 clone은
  수정하지 않고 거부한다. source 전문은 계속 `.vibecutter/` 아래 gitignored 상태다.
- **GPU queue 의미:** `targets/runtime_batches/gpu_3way.yaml`의 7/7/6 분할은 runtime 배치 위치다.
  폐지된 P1/P2/P3 audit 업무 분담을 되살리는 문서가 아니다. `runtime/gpu_preflight.py`는
  각 서버에서 자기 worker ID의 target만 source lock/static readiness/Docker/port 점검하며,
  원격 SSH dispatcher나 scheduler는 제공하지 않는다.
- **provisioning coverage:** repeatable role-based IDOR verifier 계약은 20개 중 5개에 있다.
  `c1-05`, `c1-06`, `c2-01`, `c2-02`는 self-signup/bearer, `c2-04`는 fixture-file이다.
  나머지 15개도 scan 대상이지만 실제 권한 경계 재현에는 P3가 찾은 endpoint/resource와
  안전한 role/seed 계약이 추가로 필요하다.
- **P4 임계경로:** GPU와 모델 서빙 준비보다 실제 라벨 trajectory 양이 현재 병목이다. P2는
  P3가 생성한 실앱 run ID와 연결된 runtime 정보만 제공하고, dataset export/QLoRA 평가는 P4가 맡는다.

## 보수적 진척 판단

| 범위 | 판단 | 근거 |
|---|---:|---|
| P2가 독립적으로 통제하는 구현·인프라 | 완료 | manifest 22, source lock 22/22, GPU runtime 20/20, worktree/overlay/reset/sweep/test-runner/preflight/restore 구현과 회귀 통과 |
| P2 할당 임무의 팀 통합 완료도 | 90~95% | 남은 것은 P1 baseline-restore 배선과 P3 추가 실앱 run 기반 최종 teardown 확인으로, P2 단독으로 완료 판정할 수 없음 |
| 프로젝트 전체 | 70~75% | orchestration/runtime/기본 closed-loop는 있으나 20개 audit breadth, 실제 trajectory 수량, GPU 학습·평가, 타 역할 8개 회귀 실패가 남음 |

여기서 GPU **20/20 ready**는 target runtime이 source/Docker/port 관점에서 실행 가능하다는 뜻이다.
20개 모두에서 취약점 검증·패치·FIXED가 끝났다는 뜻은 아니다.

## 변경 파일

- `runtime/target_service.py`: run-scoped overlay reset 및 stale overlay sweep 제공.
- `runtime/source_lock.py`, `targets/source-lock.yaml`: 22개 runtime manifest의 canonical repo/exact
  commit 계약과 exact coverage 검증.
- `runtime/source_bootstrap.py`: 승인형 missing-only clone bootstrap 및 기존 clone 무변경 검사.
- `runtime/catalog.py`, `runtime/worktree.py`, `runtime/test_runner.py`: contract source commit과
  run worktree/regression을 locked revision에 고정.
- `runtime/gpu_preflight.py`, `runtime/batch_queue.py`: worker assignment, source, Docker,
  available/listening port의 local-only preflight.
- `runtime/target_service.py`: `restore_baseline_after_write()`로 approved reset → start → health →
  fixture-file 재생성 또는 self-signup fresh baseline 복구.
- `runtime/run_overlay.py`: target/run별 결정적 Compose project name과 generated artifact 정리 제공.
- `targets/compose/26s-w1-c1-05.yaml`: MySQL init/grant 완료를 확인하는 readiness로 보강.
- `targets/verifier_provisioning.yaml`: `26s-w1-c1-06` self-signup/bearer provisioning 등록.
- `tests/test_target_service.py`, `tests/test_run_overlay.py`, `tests/test_c1_05_runtime_contract.py`,
  `tests/test_verifier_provisioning.py`: 위 P2 runtime 계약 회귀.

## 공통 계약 결정 — candidate 하나당 worker Run 하나

P2는 P1의 **candidate마다 새 Run** 제안을 지지한다. `VERIFIED`는 `LOCALIZING`으로만
진행하는 고정 상태이므로, 하나의 Run이 여러 verified candidate를 담는 컨테이너가 되면
상태 머신·patch·validation·evidence가 서로 섞인다. P2의 worktree와 generated Compose overlay도
이미 `target_id + run_id` 단위이므로 이 선택이 격리 및 reset 경계와 일치한다.

P1 batch/orchestrator가 구현할 최소 계약은 다음과 같다.

1. scan Run은 후보를 수집하는 batch 부모로만 사용하고 `CANDIDATE_SCAN`에서 종료한다.
2. 후보마다 별도 worker Run을 만들고, worker Run에 속한 Candidate를 materialize한 뒤 verify 한다.
   원본 scan candidate ID는 additive lineage 필드(권장: `origin_candidate_id`) 또는 동등한
   trajectory metadata로 보존한다. 기존 Candidate의 `run_id`를 덮어쓰지 않는다.
3. Finding, Observation, Patch, Validation, Trajectory는 모두 worker Run에만 저장한다.
   따라서 한 worker Run은 verified이면 patch loop로, rejected이면 종료로 독립 진행한다.
4. 같은 fixed host port를 쓰는 현재 P2 manifest는 worker Run을 **target별 순차 실행**해야 한다.
   병렬 worker runtime이 필요하면 P2에 port allocation 계약을 별도로 요청한다.

P3 verifier는 derived Candidate의 typed `attack_params`를 그대로 소비하면 된다. P3가
scan candidate ID를 report/dataset에서 추적해야 하면 위 lineage 값을 사용해야 하며, worker
Run의 evidence와 섞지 않는다.

## P2 runtime 호출 조건

- batch 시작 전: `sweep_stale_run_overlays(target_id, active_run_ids=..., approved=True)`.
  이 함수는 P2가 생성한 trusted overlay artifact만 정리하며 임의 `vc-*` project는 삭제하지 않는다.
- approved patch가 overlay를 생성한 worker Run 종료 `finally`: `reset_run(target_id, run_id,
  approved=True)`. reset 실패 시 artifact/worktree를 남겨 진단·재시도를 가능하게 한다.
- patch 전 base runtime은 target별로 공유할 수 있으나, write verifier 뒤에는 P2 target reset과
  fixture 재준비가 필요하다. 인증/상태변화 fixture는 P3가 target·safe mutation·원복 방법을
  명시해 요청할 때만 P2가 추가한다.

## 제공 인터페이스

- 입력: policy-allowed `target_id`, active worker `run_id` 집합, local `worker_id`, 명시 승인.
- 출력: source `SourceCheck`, worker `WorkerRuntimePreflight`,
  `BaselineRestoreResult`, `StaleRunSweepResult` 및 run-scoped cleanup 결과.
- 실패/예외: approval 누락, queue 밖 target, dirty/mismatched source, untrusted artifact path,
  reset/start/health/fixture 실패는 typed 상태 또는 명확한 예외로 반환한다.

## 검증

- main `63e5ffc`의 P2 source/runtime/preflight/restore 회귀는 **72 passed + 3 subtests passed**,
  P2 변경 파일 ruff와 compileall도 통과했다. 로컬 source lock 실측은 manifest 22개 모두 `ready`, contract
  `source_commit` 22개 모두 populated였다.
- 전체 `pytest -q --tb=short`는 **515 passed, 8 failed, 15 subtests passed**였다. 실패는 P4 RAG 3건,
  P1 DB migration cleanup 3건, P3 XSS vendored-path filter 1건, P1 report artifact URI 1건이며
  P2 소유 테스트 실패는 0건이다.
- 기존 실측: c1-05 fresh volume 3회 health PASS, run-scoped overlay build/start/health/reset PASS,
  stale c2-04/c3-09 overlay cleanup PASS.

### D5 main `579f28c` 재실측 (P2 단독)

- Docker Desktop Linux engine을 사용해 `26s-w1-c1-05`의 managed stale overlay sweep을 실행했다.
- fresh-volume lifecycle: 승인 reset → manifest build → start를 실행했고, MySQL·Spring app·frontend가
  모두 healthy가 됐다. loopback `GET /api/health`와 frontend `/`는 각각 **200**이었다.
- clean run worktree `p2-d5-overlay-live-20260720`를 만들고 generated Compose overlay를
  build → start했다. 같은 API/UI health **200**과 Compose 3개 service healthy를 확인했다.
- `reset_run(target_id, run_id, approved=True)`가 **True**를 반환했고, 뒤이어 generated Compose
  artifact, worktree, `14006/14007` listen socket이 모두 사라짐을 확인했다.
- P2/P1 worker-run 접점 회귀는 기존 evidence DB를 보존한 상태에서 **36 passed**했다. 새
  `origin_candidate_id` additive migration이 실제 local DB에 적용됐으며 기존 Candidate row 890개가
  유지됐다.
- 두 번째 clean worktree overlay `p2-d5-regression-20260720`에서 manifest
  `backend_regression`을 실행해 **passed**(exit 0, 약 49초)로 확인했다. reset 뒤 worktree,
  generated artifact, `14006/14007` socket이 다시 모두 제거됐다.
- holdout runtime `26s-w1-c3-09`도 stale sweep → fresh reset → build/start를 실행했다.
  MySQL·server·frontend가 모두 healthy이고 API `/api/health`와 frontend `/`가 각각 **200**,
  manifest `smoke`가 **passed**(exit 0)했다. 승인 reset 후 `14036/14037` listen socket 및
  해당 Compose container가 남지 않음을 확인했다.
- 세 GPU를 `63e5ffc`로 fast-forward하고 P2 전용 Python 3.13.14 venv를 별도로 만들었다.
  기존 모델 serving/Python 3.10 환경은 변경하지 않았다. worker-local listening preflight는
  GPU-1 **7/7**, GPU-2 **7/7**, GPU-3 **6/6**, 합계 **20/20 ready**였다.
- legacy role fixture env가 없는 6개 target은 runtime blocker가 아닌 provisioning warning으로
  분리했다: `c1-03`, `c1-05`, `c1-06`, `c1-07`, `c2-01`, `c2-02`. 이 중 self-signup 계약이
  있는 target은 P3가 ephemeral account를 사용하고, 나머지는 실제 verifier 요청이 있을 때만 보강한다.

## 다른 역할에 필요한 사항

- P1: write verifier 완료 뒤 승인된 `restore_baseline_after_write(target_id, approved=True)`를
  orchestration mutation gate에 연결해 달라. source bootstrap을 MCP로 노출한다면 P1 policy에
  typed `bootstrap_target(target_id)`를 먼저 추가해야 한다. Windows
  `tests/test_db_migration.py`의 DB handle cleanup 3건과 report artifact URI 회귀 1건을
  P1 범위에서 확인해 달라.
- P3: 각 target이 배정된 GPU에서 listening preflight 후 허가된 20개 target을 scan하고,
  우선 provisioning이 준비된 `c2-01`, `c2-02`, `c1-06`의
  추가 IDOR verify 결과와 실제 audit run ID를 남겨 달라. 별도 write fixture가 필요하면
  safe method/path/body, `observe_path`, 원복 방법을 P2에 전달해 달라.
- P4: 실제 audit run allowlist로 테스트 생성 trajectory를 제외한 뒤 export/학습을 진행하고,
  dataset row 수와 label 분포를 공유해 달라. RAG 회귀 3건도 P4 범위에서 확인해 달라.

## P2 다음 작업 계획

1. **완료 — source 재현성:** 22개 exact source lock, safe bootstrap, locked worktree와
   contract `source_commit` 배선을 완료했다.
2. **완료 — GPU preflight:** 세 서버 전용 Python 3.13.14 환경과 local-only 7/7/6 preflight를
   배치해 20/20 listening 상태를 확인했다. 원격 dispatcher는 만들지 않는다.
3. **조건부 — fixture 지원:** P3가 구체적인 role/resource/endpoint 계약을 주는 target부터 no-secret
   provisioning을 확장한다. 검증되지 않은 fixture를 15개 target에 일괄 추정해 넣지 않는다.
4. **구현 완료 / P1 배선 대기 — write verifier 위생:** shared baseline 복구 API와 stale fixture
   제거를 구현했다. `reset_run`은 patched overlay 전용이고 baseline restore는 별도 승인 단계다.
5. **P3 결과 대기 — 최종 통합 확인:** P3의 추가 실앱 run이 나오면 한 target에서
   `register → build/start → scan → worker verify → evidence/report → reset`을 재실행하고,
   포트·overlay·worktree 잔여 0을 확인한 뒤 P2 handoff를 종료한다.

## 결정·가정·리스크

- P2는 P1/P3 소유인 state machine, Candidate schema, verifier, orchestration 코드를 직접 변경하지
  않는다. 이 handoff는 해당 변경의 명시적 합의 입력이다.
- `reset_run`은 run-scoped patched overlay용이다. overlay가 없는 scan/verify worker에 무조건 호출하지
  말고, P1은 overlay 생성 여부를 기준으로 호출해야 한다.
- write verifier가 shared baseline의 데이터를 변경했다면 `reset_run`만으로는 원복되지 않는다.
  P1은 승인 gate 뒤 `restore_baseline_after_write()`를 호출해야 한다.
- 병렬 Compose worker는 현재 고정 host port와 충돌한다. 새 port allocation/URL contract 없이 병렬화하지
  않는다.

## GPU 3-way runtime batch 준비 (2026-07-20)

- `targets/runtime_batches/gpu_3way.yaml`에 policy allowlist의 실제 실행 대상 **20개**를 `gpu-1` 7개,
  `gpu-2` 7개, `gpu-3` 6개로 정확히 한 번씩 배정했다. queue loader는 누락·중복·allowlist 밖 ID를
  거부한다. 서버 주소/credential/외부 URL은 queue에 저장하지 않는다.
- 세 CAMP GPU 서버 모두 RTX 3090 24GB, Docker Engine 29.1.3이며 2026-07-20 현재 root filesystem
  여유는 GPU-1/2/3 순서로 **24GB/51GB/49GB**다. 세 checkout은 모두 main `63e5ffc`, clean 상태다.
- 모델 serving용 Python 3.10과 분리해 `/opt/VibeCutter/.venv-p2`에 Python 3.13.14와 P2 runtime
  최소 의존성을 설치했다.
- 세 서버에 main checkout과 배정된 managed target source를 bootstrap했고, queue의 20개 대상 모두
  Docker Compose `build` 및 `up --wait`를 통과했다(2026-07-20 재확인).
- GPU-1: `c1-02`, `c1-05`, `c2-02`, `c2-07`, `c3-03`, `c3-06`, `c3-09` — 모든 서비스 healthy.
- GPU-2: `c1-03`, `c1-06`, `c2-01`, `c2-04`, `c2-08`, `c3-04`, `c3-08` — 모든 서비스 healthy.
- GPU-3: `c1-04`, `c1-07`, `c2-05`, `c2-06`, `c3-02`, `c3-05` — 모든 서비스 healthy.
- `python -m runtime.gpu_preflight --worker-id gpu-N --expect-port-state listening` 실측은
  GPU-1 7/7, GPU-2 7/7, GPU-3 6/6으로 source revision, Docker, isolation/static readiness,
  listening port가 모두 통과했다. 6개 role fixture env 누락은 별도 warning이며 runtime 실패가 아니다.
- `c3-09`는 Gradle wrapper 외부 다운로드가 차단되어 공식 `gradle:9.5.1-jdk17` builder를
  사용하도록 P2 Dockerfile을 수정한 뒤 Spring build 성공 및 server/frontend/MySQL healthy를 확인했다.
- `base_url`은 계속 서버 내부 `127.0.0.1`로 유지하며, P1/P3 audit loop는 target이 배정된 동일
  서버에서 실행해야 한다. 고정 포트가 이미 할당되어 있으므로 실행 종료 후 P2 reset/sweep를 호출한다.
